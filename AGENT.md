# Agent 协作规范

## 项目定位与当前状态

仓库名为 SkillBenchmark，SkillLab 是产品工作名，`skilllab` 是拟定 CLI 名称。
项目用于跨平台 Skill 评测与迭代。当前已实现 P0 本地 mock 链路，以及 P1-P4 的首版工程实现：真实 CLI Adapter、临时环境、并发/重试调度、统计与门禁、训练证据演化、候选发布注册表和 MCP/插件入口。真实 Codex/Claude 账号、生产隔离 Worker 和平台一致性矩阵仍未在本机验证，因此 Adapter 报告默认标记为 exploratory，不把本地假执行结果当作平台成绩。

## 开始工作

1. 阅读 README.md，确认任务涉及的文档与当前交付阶段。
2. 以 docs/implementation-plan.md 确定开发顺序，以 docs/contracts.md 对齐接口，以 docs/evaluation-protocol.md 对齐评测约束。
3. 检查工作区已有修改，保留用户尚未提交的内容。
4. 仅在仓库存在 `.codegraph/` 时优先使用 CodeGraph 定位和理解代码；否则使用 `rg`。不要自行建立索引。

## 修改规范

- 项目说明使用简体中文，代码标识符与协议字段使用英文；保持既有术语一致。
- 将设计提案、已实现能力、实测结论明确区分；示例命令不能描述成已可运行。
- 当前不为规划中的模块创建空目录或无用途的依赖配置。引入实现时同步补充运行方式、依赖锁文件与相关验证。
- 架构边界或字段变化同步修改相关设计文档，避免入口说明与契约矛盾。
- 使用 UTF-8、LF 和文件末尾换行；遵循 .editorconfig。

## 评测不变量

- 固定任务、模型、平台配置、工具和预算后比较 Skill 条件，保留完整快照和证据。
- 被测执行器、独立评分器与迭代器职责分离；隐藏评分资料及最终 test 证据不能进入迭代器上下文。
- Agent 自述完成不等于通过；指标缺失使用 null/unknown，不填造数据。
- 不修改导入的 Skill 源目录；候选版本与显式发布、平台导出分开处理。
- 凭证、本地运行轨迹和私有评测资料不纳入版本控制；可公开固定样例应显式放入 suites/ 或 tests/。

## 验证与交付

文档变更检查相对链接、术语一致性、Markdown 代码块和 `git diff --check`。当前代码使用 `npm test` 验证 mock pipeline、预算边界、快照摘要、符号链接隔离、真实子进程 Adapter、原生模式能力边界、演化、发布、MCP 和 CLI 端到端链路。
实现代码后执行与变更有关的真实检查；没有真实平台配置时，不声称获得了真实测评成绩。
交付说明包括修改内容、验证结果和仍未实现或受阻的事项。真实平台运行需另行提供认证、模型和隔离 Worker 配置；没有这些配置时不能声称获得真实测评成绩。遵循 CONTRIBUTING.md 的提交约定。
